pipeline {
  agent any

  tools { jdk 'jdk17'; maven 'maven3' }

  options { timestamps(); skipDefaultCheckout(true) }

  stages {
    stage('Checkout') { steps { checkout scm } }

    stage('Build & Test') {
      steps {
        bat 'mvn -B -DskipTests=false clean package'
      }
      post {
        success {
          archiveArtifacts artifacts: 'target\\smart-study-app-*.jar', fingerprint: true
          junit allowEmptyResults: true, testResults: '**\\target\\surefire-reports\\*.xml'
        }
      }
    }

    stage('Deploy (Local Windows, background)') {
  environment { APP_DIR = 'C:\\smartstudy' }
  steps {
    withEnv([
      "GEMINI_API_KEY=${GEMINI_API_KEY}",
      "DB_USER=${DB_USER}",
      "DB_PASS=${DB_PASS}"
    ]) {
      bat '''
      echo Preparing deployment folders...
      if not exist "%APP_DIR%" mkdir "%APP_DIR%"
      if not exist "%APP_DIR%\\uploads" mkdir "%APP_DIR%\\uploads"
      if not exist "%APP_DIR%\\temp" mkdir "%APP_DIR%\\temp"
      if not exist "%APP_DIR%\\lucene-index" mkdir "%APP_DIR%\\lucene-index"

      echo Copying jar...
      copy /Y target\\smart-study-app-1.0.0.jar "%APP_DIR%\\app.jar"

      echo Stopping any existing SmartStudy process...
      for /f "tokens=2" %%a in ('netstat -ano ^| findstr :8081 ^| findstr LISTENING') do taskkill /F /PID %%a 2>nul
      ping 127.0.0.1 -n 3 > nul

      echo Starting new SmartStudy instance as detached process...
      '''
      
      powershell '''
      $psi = New-Object System.Diagnostics.ProcessStartInfo
      $psi.FileName = "javaw"
      $psi.Arguments = "-jar C:\\smartstudy\\app.jar"
      $psi.WorkingDirectory = "C:\\smartstudy"
      $psi.UseShellExecute = $false
      $psi.CreateNoWindow = $true
      [System.Diagnostics.Process]::Start($psi) | Out-Null
      Write-Output "SmartStudy process started and detached"
      '''
      
      bat '''

      echo Waiting for app to start...
      ping 127.0.0.1 -n 15 > nul

      echo Checking if app is running...
      netstat -ano | findstr :8081 | findstr LISTENING >nul && echo SUCCESS: App is listening on port 8081 || echo WARNING: App may not have started yet
      '''
    }
  }
}



    stage('Smoke Test') {
      steps {
        bat 'echo Deployment complete. App running at http://localhost:8081'
      }
    }
  }

  environment {
    GEMINI_API_KEY = credentials('gemini_api_key')
    DB_USER        = credentials('db_user')
    DB_PASS        = credentials('db_pass')
  }
}
